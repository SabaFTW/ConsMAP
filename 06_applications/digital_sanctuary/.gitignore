# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Codeberg pages deploy clone
.codeberg-deploy/

# SEALED / local-only: the full iceberg reader carries named-individual + victim
# material woven through nearly every chapter. Held under the corpus firewall —
# integrated for LOCAL preview only, never committed or deployed. The deploy script
# also strips dist/iceberg. Do not un-ignore without an explicit decision to lift the seal.
public/iceberg/

# Netlify CLI machine-local state. state.json holds the siteId link and
# .netlify/netlify.toml is a generated cache containing absolute paths from
# whichever machine last ran the CLI. The authoritative Netlify configuration
# is the netlify.toml at the repository root.
.netlify/
